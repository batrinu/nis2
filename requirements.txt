# NIS2 Compliance Assessment System - Requirements

# Core dependencies
pydantic>=2.0.0
pyyaml>=6.0

# CLI and UI
rich>=13.0.0

# Data processing
python-dateutil>=2.8.0

# Security dependencies (2026 hardening)
# Pass 7: PyYAML SafeLoader (CVE-2026-24009 pattern)
pyyaml>=6.0.1
# Pass 8: XML XXE prevention
defusedxml>=0.7.1
# Pass 14: Dependency security pins
setuptools>=70.0.0  # Fixes multiple CVEs
wheel>=0.43.0

# Device connector - SECURITY: >=3.4.0 fixes Terrapin attack
paramiko>=3.4.0
# SECURITY: >=46.0.5 fixes CVE-2026-26007 (elliptic curve subgroup validation)
cryptography>=46.0.5

# Optional: For PDF generation (if needed in future)
# weasyprint>=59.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
mypy>=1.0.0
